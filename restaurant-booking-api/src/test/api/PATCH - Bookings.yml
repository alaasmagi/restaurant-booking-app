info:
  name: PATCH - Bookings
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:8080/api/bookings/{ID}/cancel
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
