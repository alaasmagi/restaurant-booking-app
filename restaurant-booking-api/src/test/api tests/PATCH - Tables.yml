info:
  name: PATCH - Tables
  type: http
  seq: 11

http:
  method: PATCH
  url: http://localhost:8080/api/tables/{ID}/position
  body:
    type: json
    data: |-
      {
        "x": 10.5,
        "y": 20.3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
