info:
  name: GET - Bookings(ID)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/bookings/{iD}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
